<IfModule mod_rewrite.c>
    RewriteEngine On

    # If accessing public/ directly, do nothing
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirect everything else to public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
